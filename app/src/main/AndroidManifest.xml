<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="https:schemas.android.com/tools"
    package="com.example.medmanage.activities">

    <application

        android:label="@string/app_name"
        android:theme="@style/Theme.MedManage">

        <activity android:name=".activities.ViewProfileDetailsActivity"
            android:exported="false"/>

        <activity android:name=".activities.ViewMedicationActivity"
            android:exported="false"/>

        <activity android:name=".activities.FoodViewActivity"
            android:exported="false" />

        <activity android:name=".activities.DashboardActivity"
            android:exported="false" />

        <activity android:name=".activities.SignupActivity"
            android:exported="false" />

        <activity android:name=".activities.LoginActivity"
            android:exported="false" />

        <activity android:name=".activities.UpdateProfileActivity"
            android:exported="false" />

        <activity android:name=".activities.EditFoodActivity"
            android:exported="false" />

        <activity android:name=".activities.ViewAppointmentActivity"
            android:exported="false" />

        <activity android:name=".activities.ReviewAppointmentActivity"
            android:exported="false"/>

        <activity android:name=".activities.ScheduleAppointmentActivity"
            android:exported="false"/>

        <activity android:name=".activities.MainScreenActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>